@page
@model M_Chat.UI.Pages.Diagnosticos.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Diagnostico[0].Resultado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Diagnostico[0].Infante)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Diagnostico[0].Cuestionario)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Diagnostico) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Resultado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Infante.Apellido)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cuestionario.CuestionarioId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DiagnosticoId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.DiagnosticoId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.DiagnosticoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
